Mind Map generated by NB MindMap plugin   
> __version__=`1.1`,showJumps=`true`
---

# ATAM（非功能质量属性）

## 缓存
> leftSide=`true`


### 部署结构

#### 复制模型

#### 失效转移

#### 持久策略

#### 淘汰策略

#### 线程模型

#### 预热方法

#### 哈希分片策略

### 容量与性能

#### 缓存内容大小

#### 缓存内容的数量

#### 缓存的数据结构

#### 每秒的读峰值

#### 每秒的写峰值

### 其他

#### 冷热数据比例

#### 是否可能发生缓存穿透

#### 是否有大对象

#### 是否用缓存实现分布式锁

#### 是否使用缓存支持的脚本

#### 是否避免了竞争条件

#### 缓存分片方法

## 数据库
> leftSide=`true`


### 部署结构

#### 复制模型

#### 失效转移模型

#### 容灾策略

#### 归档策略

#### 读写分离策略

#### 分库分表策略

#### 静态数据和半静态数据是否使用缓存

#### 缓存穿透并压垮数据库的情况

#### 缓存失效和缓存数据预热策略

### 容量和性能

#### 当前的数据容量

#### 每天的数据增量

#### 每秒的读峰值

#### 每秒的写峰值

#### 每秒的事务量峰值

### 其他

#### 重要查询是否使用索引

#### 是否存在大数据量的查询和范围查询

#### 多表关联是否使用索引

#### 是否可以使用乐观锁取代悲观锁

#### 是否可以利用数据库内置的行级锁

#### 事务和一致性级别

#### JDBC数据源参数配置

#### 是否开启JDBC诊断日志

#### 是否使用存储过程

#### 伸缩策略

#### 水平分库分表实现方法

## 非功能性质量属性

### 核心

#### 高性能

#### 可伸缩性

#### 可扩展性

#### 安全性

#### 可用性

### 非核心

#### 可监控性

#### 可测试性

#### 鲁棒性

#### 可维护性

#### 可重用性

#### 易用性

## 应用服务器

### 部署结构

#### 负载均衡策略

#### 高可用策略

#### I/O模型

#### 线程池模型

#### 线程池中的线程数量

#### 是否多业务混合部署

### 容量和性能

#### 每天请求量

#### 接口访问峰值

#### 平均请求响应时间

#### 最大的请求响应时间

#### 在线的用户量

#### 请求的大小

#### 网卡的I/O流量

#### 磁盘的I/O流量

#### 内存使用情况

#### CPU使用情况

### 其他指标

#### 请求的内容是否包含大对象

#### GC收集器的选型和配置

## 消息队列

### 部署结构

#### 复制模型

#### 失效转移

#### 持久策略

### 容量和性能

#### 每天的数据增量

#### 消息持久的过期时间

#### 每秒的读峰值

#### 每秒的写峰值

#### 每天消息的大小

#### 平均延迟

#### 最大延迟

### 其他

#### 消费者线程池模型

#### 哈希分片策略

#### 消息的可靠投递

#### 消费者的处理流程和持久机制
